# Aktiver Provider: Secret aus der bestehenden Secret-Verwaltung einsetzen.
SCALEWAY_API_KEY=
SCALEWAY_API_URL=https://api.scaleway.ai/<projekt-id>/v1/chat/completions

# Nur fuer einen expliziten Rollback auf die bisherige Konfiguration behalten.
OPENROUTER_API_KEY=
